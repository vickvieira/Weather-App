import { View, Text} from 'react-native'
import React from 'react'

const Details = () => {
    return (
        <View style={{ marginTop: 60}}>
            <Text style={{textAlign: 'center'}}>Detailseeee</Text>
        </View>
    );
};

export default Details;